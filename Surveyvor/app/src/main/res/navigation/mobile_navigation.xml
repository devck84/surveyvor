<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
            xmlns:app="http://schemas.android.com/apk/res-auto"
            xmlns:tools="http://schemas.android.com/tools"
            android:id="@+id/mobile_navigation"
            app:startDestination="@+id/nav_home">

    <fragment
            android:id="@+id/nav_my_profile"
            android:name="es.system.dc.ui.profile.MyProfileFragment"
            android:label="My Profile"
            tools:layout="My Profile"/>

    <fragment
            android:id="@+id/nav_home"
            android:name="es.system.dc.ui.home.HomeFragment"
            android:label="Surveys"
            tools:layout="Surveys"/>

    <fragment
            android:id="@+id/nav_gallery"
            android:name="es.system.dc.ui.team.TeamFragment"
            android:label="Teams"
            tools:layout="Teams"/>

    <fragment
            android:id="@+id/nav_slideshow"
            android:name="es.system.dc.ui.friend.FriendFragment"
            android:label="Friends"
            tools:layout="@layout/fragment_friend"/>

    <fragment
            android:id="@+id/nav_pending_invitation"
            android:name="es.system.dc.ui.friend.PendingInvitationFragment"
            android:label="Pending Invitation"
            tools:layout="Pending Invitation"/>
</navigation>